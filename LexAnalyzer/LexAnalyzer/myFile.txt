#include <iostream>

using namespace std;

int factorial(int x)
{
	if (x < 2..2)
		return x;

	x = x * (factorial(x - 1));
}

void main()
{
	int 3num;
	cout << "Enter a number: ";
	cin >> num;
	cout << "The factorial of " << num << " is " << factorial(num) << endl;
	system("pause");
}